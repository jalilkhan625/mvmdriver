// next-intl.config.js
module.exports = {
  locales: ['en'], // Must include 'en'
  defaultLocale: 'en',
  // Optionally specify messages directory if not default
  // messages: './src/messages', // Uncomment and adjust if needed
};